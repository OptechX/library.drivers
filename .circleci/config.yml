version: 2.1

orbs:
  win: circleci/windows@5.0

jobs:

  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"

  microsoft:
    executor: win/server-2022
    steps:
      - checkout
      - run:
          name: Install Selenium
          no_output_timeout: "10m"
          command: pwsh -Command "Install-Module Selenium"
          
workflows:
  # say-hello-workflow:
  #   jobs:
  #     - say-hello
  my-workflow:
    jobs:
      - microsoft
